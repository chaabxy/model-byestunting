{
  "name": "stunting-prediction-api",
  "version": "1.0.0",
  "description": "API untuk prediksi stunting",
  "main": "api/health.js",
  "scripts": {
    "build": "echo 'No build step required for serverless functions'"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.15.0"
  },
  "engines": {
    "node": "18.x"
  }
}
