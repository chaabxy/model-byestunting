{
  "name": "stunting-prediction-api",
  "version": "1.0.0",
  "description": "API untuk prediksi stunting menggunakan model ML",
  "type": "module",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for serverless functions'"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.17.0"
  }
}
